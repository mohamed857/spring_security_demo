spring.application.name=spring-sec-demo
#spring.security.user.name=mohamed
#spring.security.user.password=1234

#server.servlet.session.cookie.same-site=strict

spring.datasource.url=jdbc:postgresql://localhost:5432/test
spring.datasource.username=postgres
spring.datasource.password=570001
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Reduce memory footprint and improve performance in production
spring.main.lazy-initialization=true
spring.jpa.open-in-view=false

# HTTP compression for common types
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain,text/css,application/javascript
server.compression.min-response-size=1024

# JWT secret can be provided via env var APP_JWT_SECRET (base64)
# app.jwt.secret=

